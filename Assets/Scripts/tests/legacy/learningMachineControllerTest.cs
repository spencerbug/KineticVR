using System;
using System.Collections.Generic;
using System.Threading;
using UnityTest;
using NUnit.Framework;
using UnityEngine;

namespace UnityTest
{
	[TestFixture]
	[Category("learningMachineController")]
	internal class learningMachineControllerTest {
		[Test]
		public void testCoroutineLerp(){
			//stateMachine StateMachine = new stateMachine();
			Assert.IsTrue(true);
		}

		//Assert.areEqual(StateMachine.currentSpeed, 0D);
	}
}